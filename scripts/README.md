# scripts/

Scripts utilitaires pour AndroidAPN.

## Contenu
- `generate_apn_db.py` : génère un fichier `apn.conf` depuis une source APN.
- `install.sh` : installe l'environnement Python et les dépendances.

## Notes
Les scripts utilisent automatiquement la configuration de `config/logging.conf`.
